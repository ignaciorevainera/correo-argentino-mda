---
import { Icon } from "astro-icon/components";

interface Props {
  title?: string;
  description?: string;
  icon?: string;
  class?: string;
  titleClass?: string;
  id?: string;
}

const {
  title,
  description,
  icon,
  class: className,
  titleClass,
  id,
} = Astro.props;
---

<section
  id={id}
  class:list={[
    "card card-compact bg-base-100 border-base-300 border shadow-sm",
    className,
  ]}
>
  {
    (title || icon) && (
      <div class="card-body pb-0">
        <div class="flex items-center gap-2">
          {icon && (
            <Icon name={icon} class="text-secondary shrink-0" size={20} />
          )}
          {title && (
            <h2
              class:list={[
                "text-base-content text-lg font-bold tracking-tight",
                titleClass,
              ]}
            >
              {title}
            </h2>
          )}
        </div>
        {description && (
          <p class="text-base-content/60 mt-1 text-sm">{description}</p>
        )}
      </div>
    )
  }
  <div class="card-body">
    <slot />
  </div>
</section>
